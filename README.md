
  # Official Website for Shielify

  ## Running the code

  Run `bun install` to install the dependencies.

  Run `bun run dev` to start the development server.
  
  Run `bun run build` to build and package the production bundle.

  Run `bun run start` to start the production mode.
